## Packages
framer-motion | Smooth scroll animations and page transitions
react-hook-form | Form state management
@hookform/resolvers | Zod validation integration for forms
zod | Schema validation
lucide-react | High-quality icons for UI

## Notes
- Using framer-motion for modern SaaS-style entrance and scroll animations.
- The contact form submits to `/api/contact` using the exact schema defined in `@shared/routes`.
- Font configurations require loading Plus Jakarta Sans and Inter from Google Fonts.
- Using Shadcn UI components which are already available in the environment.
